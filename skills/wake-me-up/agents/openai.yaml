interface:
  display_name: "Wake Me Up"
  short_description: "Arm guarded monitor instead of waiting"
  default_prompt: "Use $wake-me-up: reserve before my launcher starts the producer, then arm its returned monitor_condition after launch."
